{% extends 'base.html.twig' %}

{% block title %}RecipeIngredients index{% endblock %}

{% block body %}
<h1>RecipeIngredients index</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Ingredient</th>
            <th>Quantity</th>
            <th>Unit</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
    {% for recipe_ingredient in recipe_ingredients %}
        <tr>
            <td>{{ recipe_ingredient.id }}</td>
            <td>{{ recipe_ingredient.ingredient ? recipe_ingredient.ingredient.ingredient : 'No ingredient' }}</td>
            <td>{{ recipe_ingredient.quantity }}</td>
            <td>{{ recipe_ingredient.unit }}</td>
            <td>
               
                <a href="{{ path('app_recipe_ingredients_edit', {'id': recipe_ingredient.id}) }}">edit</a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="5">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<br>
<a href="{{ path('app_recipe_ingredients_new') }}">Create new</a>
<br>
<br>

<a href="{{ path('app_recipes_new') }}">back to list</a>

{% endblock %}
